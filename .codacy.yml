engines:
  duplication:
    exclude_paths:
      - 'test/**'
      - 'msvc/llamalog_Test/**'
  metric:
    exclude_paths:
      - 'test/**'
      - 'msvc/llamalog_Test/**'
  coverage:
    exclude_paths:
      - 'test/**'
      - 'msvc/llamalog_Test/**'
  clang-tidy:
    exclude_paths:
      - '**/test/**'
      - 'msvc/llamalog_Test/**'
languages:
  cpp:
    extensions:
      - '.h'
exclude_paths:
  - 'lib/**'
